const express = require ('express')
const app = require()
app.set('view engine', 'pug')
app.get('/', (req,res)=>){

}

app.listen(2000, err =>{
	if(err) throw err
		console.log("App is running ... ")
})
